
# ======================================================================
# 🇾🇪 YemenJPT Platform Environment Configuration (V25.0 - Enterprise Edition)
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to '.env' (`cp .env.example .env`).
# 2. Fill in ALL required values. Use a password manager for strong passwords.

# --- Domain & Network (Required) ---
# The primary domain the platform will run on. All services will be subdomains of this.
# For local development, use 'localhost'. For production, use your actual domain.
DOMAIN=localhost

# --- Unified Passwords (Required) ---
# To simplify setup, some services can share a strong password.
# It is highly recommended to generate strong, unique passwords for each in production.
UNIFIED_PASS=generate_a_strong_password
MARIADB_ROOT_PASSWORD=generate_another_strong_password
POSTGRES_PASSWORD=generate_a_postgres_password

# --- PostgreSQL Configuration (Required) ---
# Credentials for the main PostgreSQL database which serves multiple applications.
POSTGRES_USER=yemenjpt_user
POSTGRES_DB=yemenjpt_db

# --- Keycloak Identity Provider (Required) ---
# Keycloak is the central user management system (Single Sign-On).
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=${UNIFIED_PASS} # Uses the unified password by default

# --- Ghost CMS Configuration (Required) ---
GHOST_DB_USER=ghost_user
GHOST_DB_PASSWORD=${UNIFIED_PASS}
GHOST_DB_NAME=ghost_db

# --- Poste.io Mail Server (Required) ---
# This is for the self-hosted mail server itself.
POSTE_DB_USER=poste_user
POSTE_DB_PASSWORD=${UNIFIED_PASS}

# --- Evolution API for WhatsApp (Required) ---
EVOLUTION_API_URL=http://localhost:8093
EVOLUTION_API_KEY=generate_a_strong_evolution_api_key

# --- SMTP Relay Configuration (Highly Recommended for Production) ---
# Used by services like n8n, Nextcloud, etc., to send emails (notifications, password resets).
# Using a transactional email service like SendGrid, Mailgun, or AWS SES is recommended.
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_or_ses_api_key
SMTP_FROM_ADDRESS=noreply@your.domain
SMTP_FROM_NAME="YemenJPT Platform"

# --- AI Services (Optional but Recommended) ---
# Your Google Gemini API Key. Required for the cloud AI provider (YemenJPT-Cloud).
# If left blank, the system can fall back to the local Ollama instance.
API_KEY=your_google_gemini_api_key_here

# --- Telegram Bot for Notifications (Optional) ---
# Used by the backend to send critical system alerts to the administrator.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ROOT_CHAT_ID=

# --- Dashy Admin API Key (Optional but recommended for remote panic button) ---
# Generate a random string for the panic button API key in dashy-admin.yml
ADMIN_API_KEY=generate_a_random_api_key_for_dashy
