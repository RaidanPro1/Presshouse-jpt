# ======================================================================
# 🇾🇪 YemenJPT Enterprise - Master Environment Configuration (.env.example)
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to '.env' (`cp .env.example .env`).
# 2. Update the DOMAIN if it's different from ph-ye.org.
# 3. Set your Cloudflare API token.
# 4. The unified password and S3 details are pre-configured as per your request.

# --- GLOBAL SETTINGS ---
# The primary domain the platform will run on.
DOMAIN=ph-ye.org
# Set the timezone for all containers to ensure consistent logging.
TZ=Asia/Aden

# --- UNIFIED PASSWORD ---
UNIFIED_PASSWORD=Samah@2052024X

# --- DATABASE CONFIG ---
POSTGRES_DB=yemenjpt
POSTGRES_USER=raidanpro
POSTGRES_PASSWORD=${UNIFIED_PASSWORD}

MARIADB_DATABASE=azuracast
MARIADB_USER=azuracast
MARIADB_PASSWORD=${UNIFIED_PASSWORD}
MARIADB_ROOT_PASSWORD=${UNIFIED_PASSWORD}

REDIS_PASSWORD=${UNIFIED_PASSWORD}

# --- S3 Object Storage (Contabo) ---
S3_ENABLED=true
S3_ENDPOINT=usc1.contabostorage.com
S3_REGION=us-central-1
S3_ACCESS_KEY_ID=45c5796c47d41e6c064fa73e62425930
S3_SECRET_ACCESS_KEY=149962a8d5c6031955ae499a8a5ec6bc
S3_BUCKET_NAME=raidanpro

# --- Cloudflare API ---
# Used by the install script to automatically set up DNS records.
CF_DNS_API_TOKEN=xu2gk4PQkS15RgwuPc5YWOpGkWt4rxs40hrydUMp

# --- AI CONFIG ---
# Local LLM Model to pull on first startup (e.g., llama3:8b, ar-llama:7b)
OLLAMA_MODEL=llama3:8b
OLLAMA_KEEP_ALIVE=24h

# --- MEDIA EMPIRE CONFIG ---
# Ghost CMS - Connects to the MariaDB service
GHOST_DB_HOST=mariadb
GHOST_DB_USER=${MARIADB_USER}
GHOST_DB_PASSWORD=${MARIADB_PASSWORD}
GHOST_DB_NAME=ghost
GHOST_URL=https://ghost.${DOMAIN}

# WordPress - Connects to the MariaDB service
WORDPRESS_DB_HOST=mariadb
WORDPRESS_DB_USER=${MARIADB_USER}
WORDPRESS_DB_PASSWORD=${MARIADB_PASSWORD}
WORDPRESS_DB_NAME=wordpress

# --- YEMENJPT BACKEND ---
# Used for system notifications. Get these from @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ROOT_CHAT_ID=

# --- GITEA ---
GITEA_DB_HOST=postgres:5432
GITEA_DB_NAME=gitea
GITEA_DB_USER=${POSTGRES_USER}
GITEA_DB_PASSWD=${POSTGRES_PASSWORD}

# --- Mail Server (Poste.io) ---
# Exposed ports for the mail server container to avoid conflict with Nginx Proxy Manager
HTTPS_PORT=8443
HTTP_PORT=8089
