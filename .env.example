# ======================================================================
# 🇾🇪 YemenJPT Enterprise - Master Environment Configuration (.env.example)
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to '.env' (`cp .env.example .env`).
# 2. Fill in ALL required values. Use a password manager for strong, unique passwords.
# 3. Any value left as "generate_..." will be auto-generated by the install script.

# --- GLOBAL SETTINGS ---
# The primary domain the platform will run on.
DOMAIN=ph-ye.org
# Set the timezone for all containers to ensure consistent logging.
TZ=Asia/Aden

# --- DATABASE CONFIG ---
# PostgreSQL - Used by Gitea, Label Studio, etc.
POSTGRES_DB=yemenjpt
POSTGRES_USER=raidanpro
POSTGRES_PASSWORD=generate_a_strong_postgres_password

# MariaDB - Used by AzuraCast, Ghost, WordPress
MARIADB_DATABASE=azuracast
MARIADB_USER=azuracast
MARIADB_PASSWORD=generate_a_strong_mariadb_password
MARIADB_ROOT_PASSWORD=generate_a_very_strong_mariadb_root_password

# Redis - Used by AzuraCast, n8n, etc.
REDIS_PASSWORD=generate_a_strong_redis_password

# MinIO (S3 Storage) - Access and Secret keys
MINIO_ROOT_USER=raidanpro
MINIO_ROOT_PASSWORD=generate_a_strong_minio_password

# --- AI CONFIG ---
# Local LLM Model to pull on first startup (e.g., llama3:8b, ar-llama:7b)
# Keep this small for initial setup.
OLLAMA_MODEL=llama3:8b
# How long the model stays loaded in memory after last use.
OLLAMA_KEEP_ALIVE=24h

# --- MEDIA EMPIRE CONFIG ---
# Ghost CMS - Connects to the MariaDB service
GHOST_DB_HOST=mariadb
GHOST_DB_USER=${MARIADB_USER}
GHOST_DB_PASSWORD=${MARIADB_PASSWORD}
GHOST_DB_NAME=ghost
GHOST_URL=https://ghost.${DOMAIN}

# WordPress - Connects to the MariaDB service
WORDPRESS_DB_HOST=mariadb
WORDPRESS_DB_USER=${MARIADB_USER}
WORDPRESS_DB_PASSWORD=${MARIADB_PASSWORD}
WORDPRESS_DB_NAME=wordpress

# --- CLOUDFLARE TUNNEL ---
# Your token obtained from the Cloudflare Zero Trust dashboard.
# See: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/install-and-setup/tunnel-guide/
CF_API_TOKEN=your_cloudflare_tunnel_token_here

# --- YEMENJPT BACKEND ---
# Used for system notifications. Get these from @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ROOT_CHAT_ID=

# --- GITEA ---
GITEA_DB_HOST=postgres:5432
GITEA_DB_NAME=gitea
GITEA_DB_USER=${POSTGRES_USER}
GITEA_DB_PASSWD=${POSTGRES_PASSWORD}
